{
  "name": "syncify-server",
  "version": "0.0.1",
  "scripts": {
    "client": "cd client && npm start",
    "client_build": "cd client && npm build",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "build": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client_build\""

  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "node-fetch": "^2.6.1",
    "node-hue-api": "^5.0.0-beta.2",
    "nodemon": "^2.0.7",
    "threads": "^1.6.3",
    "tiny-worker": "^2.3.0"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
